<h1>Pending Requests</h1>

<table>
<% @user_relations.each do |user_relation| %>
  <tr>
    <td><%= User.find(user_relation.request_user_id).username %></td>
    <td><%= link_to 'Accept', :action => "accept_request", :id=>user_relation.request_user_id %></td>
  </tr>
<% end %>
</table>

<br />

<%#= link_to 'New User relation', new_user_relation_path %>
